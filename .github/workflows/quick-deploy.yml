name: 🚀 Quick Deploy — Quarkus Mock Server

# ──────────────────────────────────────────
#  로컬에서 main 브랜치에 push 하는 즉시 실행
#  태그/PR 불필요 — 코드 수정 → push → 배포 완료
# ──────────────────────────────────────────
on:
  push:
    branches: ["master"]

env:
  JAVA_VERSION: "21"
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  APP_NAME: mock-server                      # ← 컨테이너 이름
  APP_PORT: 8282                             # ← 앱 포트

jobs:
  # ──────────────────────────────────────────
  #  STEP 1 : Build  (fast-jar 사용)
  # ──────────────────────────────────────────
  build-and-deploy:
    name: "Build → Docker → Deploy"
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write          # GHCR push 권한

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: temurin
          cache: maven          # ~/.m2 캐시 → 재빌드 속도 향상

      # ── 빌드 (테스트 생략 — 속도 우선) ──────────────────
      - name: Maven Build (fast-jar, skip tests)
        run: |
          mvn -B package 
           

      # ── Docker ───────────────────────────────────────────
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Push image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: src/main/docker/Dockerfile.jvm    # Quarkus 기본 생성 파일
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:sha-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # ── SSH 배포 ─────────────────────────────────────────
      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Add server to known hosts
        run: |
          mkdir -p ~/.ssh
          # 커스텀 포트로 호스트 키 스캔
          ssh-keyscan -p ${{ vars.DEPLOY_SSH_PORT }} ${{ vars.DEPLOY_HOST }} >> ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts

      - name: Deploy to server
        run: |
            ssh -p ${{ vars.DEPLOY_SSH_PORT }} \
                ${{ vars.DEPLOY_USER }}@${{ vars.DEPLOY_HOST }} bash -s << 'EOF'
              set -e
              IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest"
            
              echo "${{ secrets.GITHUB_TOKEN }}" | \
                docker login ghcr.io -u ${{ github.actor }} --password-stdin
            
              docker pull "$IMAGE"
              docker stop ${{ env.APP_NAME }} 2>/dev/null || true
              docker rm   ${{ env.APP_NAME }} 2>/dev/null || true
            
              docker run -d \
                --name ${{ env.APP_NAME }} \
                --restart unless-stopped \
                -p ${{ env.APP_PORT }}:8080 \
                "$IMAGE"
            
              docker image prune -f
              echo "✅ Done — $(docker ps --filter name=${{ env.APP_NAME }} --format '{{.Status}}')"
            EOF
      

      # ── 헬스체크 ─────────────────────────────────────────
      - name: Health check
        run: |
          echo "⏳ Waiting for app..."
          for i in {1..6}; do
            STATUS=$(curl -sf -o /dev/null -w "%{http_code}" \
              http://${{ env.DEPLOY_HOST }}:${{ env.APP_PORT }}/q/health/live 2>/dev/null || echo "000")
            [ "$STATUS" = "200" ] && echo "✅ Live! (HTTP 200)" && exit 0
            echo "  [$i/6] HTTP $STATUS — retry in 5s..."
            sleep 5
          done
          echo "❌ Health check failed"
          exit 1
